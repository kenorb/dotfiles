# Allow unprivileged user namespace creation without requiring a matching
# AppArmor profile. Overrides the distro default in
# /etc/sysctl.d/60-apparmor-namespace.conf (Ubuntu 23.10+ / AppArmor 4).
kernel.apparmor_restrict_unprivileged_userns = 0
